{
  "name": "challenge-processor-lambda",
  "version": "1.0.0",
  "description": "SQS-triggered challenge processor Lambda with cold-start config caching",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-ecs": "^3.0.0",
    "@aws-sdk/client-ssm": "^3.0.0",
    "axios": "^1.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
