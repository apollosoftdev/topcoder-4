{
  "name": "router-lambda",
  "version": "1.0.0",
  "description": "MSK to SNS router Lambda for fan-out architecture",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-sns": "^3.0.0",
    "@aws-sdk/client-dynamodb": "^3.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
