{
  "name": "submission-watcher-lambda",
  "version": "1.0.0",
  "description": "Per-challenge SQS-triggered Lambda for scoring submissions",
  "main": "index.js",
  "scripts": {
    "test": "jest --coverage"
  },
  "dependencies": {
    "@aws-sdk/client-ecs": "^3.529.1",
    "@aws-sdk/client-ssm": "^3.529.1",
    "axios": "^1.10.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "aws-sdk-client-mock": "^3.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
